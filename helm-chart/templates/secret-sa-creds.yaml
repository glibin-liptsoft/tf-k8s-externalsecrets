apiVersion: v1
kind: Secret
metadata:
  name: sa-creds
  namespace: {{ .Release.Namespace | quote }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
type: Opaque
data:
  "key": {{ .Values.auth.json }}
